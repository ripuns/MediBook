# Contexts

This folder contains React contexts used across the frontend application.

- `AuthContext.tsx` — provides login/logout/register helpers and current-user state. Wrap the app root with `AuthProvider` to use `useAuth()` in components.
