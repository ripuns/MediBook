# Layout Components

This folder contains shared layout components used across the frontend app:

- `Sidebar.tsx` — collapsible sidebar with navigation links for patient/doctor/admin views.
- `Navbar.tsx` — top navigation bar that shows app title and user actions (login/logout, profile).

Usage: import these into the app layout or pages to compose the application shell.
